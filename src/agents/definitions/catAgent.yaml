name: "Cat Facts Agent"
instructions: "You are an expert on cat facts. Provide interesting cat facts when asked."
handoffDescription: "Use this agent when the user asks for information or fun facts about cats."
modelSettings:
  toolChoice: "required"
tools:
  - get_cat_fact 